import { colors } from './colors';
import { typography } from './typography';
import { shadows, radii } from './shadows';

export { colors, typography, shadows, radii };
export default { colors, typography, shadows, radii };
